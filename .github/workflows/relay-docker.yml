name: Relay Docker image

on:
  push:
    branches: [main]
    paths:
      - "relay/**"
      - ".github/workflows/relay-docker.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./relay
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/8004swap-relay:latest
            ghcr.io/${{ github.repository_owner }}/8004swap-relay:${{ github.sha }}
